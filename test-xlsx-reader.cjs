const {chromium}=require('C:/Users/PRO/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright'),assert=require('assert/strict'),path=require('path');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage();await p.goto('file:///'+path.resolve('masal.html').replaceAll('\\','/'));await p.waitForSelector('.pagehead');await p.evaluate(()=>app.go('import'));await p.waitForTimeout(60);await p.evaluate(()=>{const w=app.$refs.operations;w.importCost=4000;w.loadPrice=4500});await p.locator('.operations-panel input[multiple]').setInputFiles('review/import-test.xlsx');await p.waitForFunction(()=>app.$refs.operations.files.length>0);const rows=await p.evaluate(()=>app.$refs.operations.files[0].rows);assert.equal(rows[1][0],'00001234567890123456');assert.equal(rows[1][2],'2030-12-31');await p.getByRole('button',{name:'فحص ومعاينة',exact:true}).click();assert.equal(await p.evaluate(()=>app.$refs.operations.preview[0].checked[0].error),'');await p.getByRole('button',{name:'اعتماد الدفعات المعروضة',exact:true}).click();assert.equal(await p.evaluate(()=>app.s.batchInvoices[0].quantity),1);console.log('PASS native XLSX shared/inline strings, preserved leading zeros, Excel date conversion, preview and posting');}finally{await b.close()}})().catch(e=>{console.error(e);process.exit(1)});
