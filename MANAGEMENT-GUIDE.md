# التقارير والصلاحيات

افتح `masal.html` مباشرة. التعديلات تظل محفوظة في متصفحك؛ البناء الجديد لا يستبدل بياناتك المحفوظة.

## مركز التقارير

26 قسمًا: المبيعات التفصيلية، حسب المنتج والوكيل والنقطة، محاولات الطباعة، التوريد والطلبيات، تتبع البطاقات دون PIN/CVC، المنتجات وحدودها، المزودون، المخزون، أرصدة المحافظ، القيود المالية، الوكلاء، الأجهزة، الأسعار، الاعتمادات، المطالبات، التصدير، الدعم وردوده، الموظفون وتخصيص صلاحياتهم، التدقيق، التشغيل، التكاملات، والإشعارات.

- فلاتر للفترة والوكيل والنقطة والمحافظة والمنتج والمزود وحالة البيع. تاريخ الفترة بتوقيت بغداد، شامل ليومي البداية والنهاية.
- الفلاتر الخاصة بالمبيعات لا تغيّر معنى التقارير الأخرى: الحالة للبيع فقط؛ المنتج والمزود للمبيعات والمخزون والأسعار. توضح كل لقطة أنها حالية وليست رصيدًا تاريخيًا.
- البحث داخل الجدول، ترتيب الأعمدة، إخفاء الأعمدة، و25/50/100 سجل في الصفحة. البحث وإخفاء الأعمدة يطبقان على تصدير الجدول الحالي؛ التصدير الشامل يتضمن كل الأقسام المطابقة لفلاتر التقرير.
- تصدير CSV لكل الأقسام أو الجدول الحالي. الملف الشامل يضم جداول متتابعة، وليس جدولًا واحدًا للاستيراد الآلي.
- تنزيل تقرير HTML منسق مستقل، أو فتح نسخة الطباعة ثم اختيار حفظ PDF من المتصفح.
- التكلفة والربح حقان منفصلان؛ تزال أعمدتهما وقيمهما من بيانات التقرير للمستخدم غير المخول. التقارير لا تصدر رموز البطاقات حتى لمدير النظام.
- الربح المعروض إجمالي قبل المصاريف التشغيلية. مخزون البطاقات مستقل عن رصيد المحفظة. التحويلات الداخلية ليست إيراد مبيعات.
- كشف المحفظة يعرض افتتاح الفترة والوارد والصادر والإقفال والرصيد الحالي بصورة منفصلة.

## منح صلاحيات لموظف

1. افتح «أنواع الصلاحيات» ثم «إضافة صلاحية». اكتب اسمًا مثل «محاسب الفرع» أو «موظف مبيعات».
2. اختر الإجراءات بمربعات ✓، منفردة أو على مستوى المجموعة أو تحديد الكل. احفظ النوع بعد مراجعة الاختيارات. يبقى نوع الصلاحية مستقلًا عن الموظفين ويمكن إسناده لأكثر من موظف.
3. من «الهوية والمستخدمون» اختر «موظف جديد»، وأدخل اسم الموظف والإيميل وكلمة المرور ونوع الصلاحية. الإيميل فريد، وكلمة المرور الجديدة لا تقل عن 8 أحرف.
4. افتح «نطاق بيانات الموظف» لاختيار الوكلاء التابعين له، مع إمكانية تضمين الوكلاء الفرعيين. دون تحديد وكلاء لا يحصل الموظف على بيانات فروع، حتى لو كان نوعه يسمح بفتح الوحدة.
5. تعديل نوع الصلاحية يطبق على جميع الموظفين المرتبطين به. تظهر أعدادهم قبل التأكيد، ويلزم سبب عند تعديل نوع محفوظ. تسجل التغييرات في التدقيق. إيقاف النوع يمنع صلاحيات جميع المرتبطين به إلى أن يعاد تفعيله.

عند تعديل الموظف اترك كلمة المرور فارغة للإبقاء على الحالية. لا تُعرض كلمة المرور القديمة؛ يخزن النظام مشتق PBKDF2-SHA256 بملح عشوائي و210,000 دورة، دون حفظ النص الأصلي أو إدراجه في سجل التدقيق. هذا لا ينشئ خادم مصادقة أو تسجيل دخول إنتاجيًا؛ المبدّل العلوي يبقى محاكاة للحسابات.

الحسابات القديمة تحتفظ بأدوارها وصلاحياتها السابقة إلى أن تسند لها نوعًا مسمى. لا يغير التحديث أرصدتها أو عملياتها. بيانات الإيميل وكلمة المرور غير الموجودة في الحسابات القديمة لا تُختلق؛ تستكمل عند تعديلها.

يوجد **145 مفتاح صلاحية** تشمل عرض الوحدات، إنشاء وتعديل وتعطيل السجلات، الإيداع والتحويل، معاينة واستيراد واعتماد المخزون، حجر وإلغاء الدفعات، اقتراح واعتماد وإرجاع الأسعار، الطباعة وإعادتها وتأكيد نتيجتها، المطالبات والتسويات، التصدير والتقارير، إدارة الموظفين، ونطاق البيانات. توجد صلاحيات إضافية لحقول مثل حدود البيع، الحد التعاقدي، بيانات الجهاز والموقع، حدود إعادة الطباعة، كل سياسة تشغيل، الشعار وقالب الوصل.

فتح الوحدة شرط لتفعيل إجراءاتها. الموظف المرتبط بنوع مسمى يأخذ الإجراءات المحددة داخل هذا النوع؛ الإجراءات غير المحددة ممنوعة. تبقى الصلاحيات القديمة للحسابات التي لم تربط بنوع مسمى. صلاحية الإجراء لا تتجاوز النطاق التنظيمي للموظف.

لا يمكن منح صلاحيات جديدة لا يملكها المنفذ، أو توسيع نطاق خارج نطاقه، أو تعديل صلاحيات الحساب الحالي أو مدير النظام من هذه الشاشة. النسخ الكاملة والاستعادة لمدير النظام فقط لأنها تحتوي بيانات جميع الفروع ورموز البطاقات. اعتماد الأسعار يبقى بواسطة مستخدم ثانٍ حتى عند منحه الصلاحية.

## حدود التنفيذ

هذه نسخة Vue محلية دون خادم أو تسجيل دخول حقيقي. التحقق في محرك العمليات والأزرار يحكم سير العمل المحلي، لكنه لا يحمي بيانات localStorage ممن يملك الجهاز أو أدوات المطور. حسابات المبدّل العلوي محاكاة أدوار. التشغيل الفعلي بين موظفين يحتاج مصادقة وخادمًا يفرض مفاتيح الصلاحيات والنطاق على كل طلب، ويعزل البيانات وسجل التدقيق في قاعدة البيانات.

## ملفات التنفيذ والتحقق

- `access.js`: تعريف المفاتيح، الأدوار الافتراضية، المنح والمنع، النطاق، حفظ التغييرات.
- `engine.js`: التحقق قبل العمليات المالية والتشغيلية.
- `reports.js`: أقسام التقرير والحسابات وفلترة البيانات والأعمدة حسب الصلاحيات.
- `enhancements.js`: واجهة الإدارة، مراجعة التغييرات، ربط الأزرار والتصدير والطباعة.
- `management.css` و`management-locales.js`: التصميم والترجمة.
- `staff-model.js` و`staff-ui.js`: أنواع الصلاحيات المشتركة، نموذج الموظف، التحقق من البريد وتخزين مشتق كلمة المرور.
- `node build.cjs`: تحديث النسخة المستقلة.
- `node test-access-reports.cjs`: تحقق قواعد الصلاحيات والنطاق وتسوية أرقام التقارير.
- `node test-management-ui.cjs`: إنشاء موظف ومنح صلاحياته، منع الإجراء والتصدير، الطباعة، حفظ الصلاحيات، واللغات والوضعين والهاتف.
- `node test-staff-profiles.cjs` و`node test-staff-ui.cjs`: إنشاء نوع مسمى وإسناده، انتقال تغييره لعدة موظفين، تكرار الإيميل، كلمات المرور وتعديلها، الحفظ وإعادة التحميل والتجاوب.
