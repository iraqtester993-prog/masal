const fs=require('fs');let s=fs.readFileSync('index.html','utf8');const start=s.indexOf('<div class="card"><form v-permit="can(\'security.policies\')"');const end=s.indexOf('</form></div>',start);if(start<0||end<0)throw Error('Missing policies panel');s=s.slice(0,start)+s.slice(end+'</form></div>'.length);s=s.replace('<template v-else-if="page===\'security\'"><div class="two">','<template v-else-if="page===\'security\'"><div>');fs.writeFileSync('index.html',s);
