const fs=require('fs');function edit(f,fn){fs.writeFileSync(f,fn(fs.readFileSync(f,'utf8')))}
edit('categories-ui.js',s=>s.replace('function install(o){',"function install(o){\n schemas.products.fields=schemas.products.fields.filter(f=>!['dailyQty','dailyAmount'].includes(f.key));").replace('<div class="category-editor formgrid">',`<div class="category-editor formgrid"><label>{{vm.tr('نوع الحد اليومي')}}<select v-model="p.dailyLimitType" required aria-label="نوع الحد اليومي"><option value="" disabled>اختر نوع الحد</option><option value="quantity">حد الكمية اليومي</option><option value="amount">الحد المالي اليومي</option></select></label><label v-if="p.dailyLimitType==='quantity'">حد الكمية اليومي<input type="number" min="1" step="1" required v-model.number="p.dailyQty" aria-label="حد الكمية اليومي"></label><label v-if="p.dailyLimitType==='amount'">الحد المالي اليومي • د.ع<input type="number" min="0.01" step="0.01" required v-model.number="p.dailyAmount" aria-label="الحد المالي اليومي"></label>`).replace('const p=this.editForm;if(this.categorySpecificCities',"const p=this.editForm;if(!['quantity','amount'].includes(p.dailyLimitType))throw Error('اختر نوع الحد اليومي');const selected=p.dailyLimitType==='quantity'?'dailyQty':'dailyAmount',n=Number(p[selected]);if(!Number.isFinite(n)||n<=0||(selected==='dailyQty'&&!Number.isInteger(n)))throw Error('أدخل حدًا يوميًا موجبًا، والكمية يجب أن تكون عددًا صحيحًا');p[selected]=n;p[selected==='dailyQty'?'dailyAmount':'dailyQty']=null;if(this.categorySpecificCities").replace("const p=this.editForm;for(const [key,v]", "const p=this.editForm;p.dailyLimitType??=(Number(p.dailyQty)>0&&!(Number(p.dailyAmount)>0)?'quantity':Number(p.dailyAmount)>0&&!(Number(p.dailyQty)>0)?'amount':'');for(const [key,v]").replace("value(p,k){if(k.startsWith", "value(p,k){if(['dailyQty','dailyAmount'].includes(k)&&!(Number(p[k])>0))return '—';if(k.startsWith"));
edit('workflow-engine.js',s=>s.replace('Math.min(old[k],limits?.[k]??Infinity,device?.[k]??Infinity)',"Math.min(old[k]===null||old[k]===undefined?Infinity:old[k],limits?.[k]??Infinity,device?.[k]??Infinity)"));
edit('engine.js',s=>s.replace('if(own.reduce((n,t)=>n+t.quantity,0)+q>p.dailyQty||own.reduce((n,t)=>n+t.total,0)+q*price>p.dailyAmount)', 'if((p.dailyQty>0&&own.reduce((n,t)=>n+t.quantity,0)+q>p.dailyQty)||(p.dailyAmount>0&&own.reduce((n,t)=>n+t.total,0)+q*price>p.dailyAmount))'));
