const fs=require('fs');let s=fs.readFileSync('operations-ui.js','utf8');const lines=s.split(/\r?\n/);const matched=lines.filter(l=>l.includes('سجل تحميل الدفعات'));if(matched.length!==1)throw Error('Expected one history section');s=lines.filter(l=>!l.includes('سجل تحميل الدفعات')).join('\n');fs.writeFileSync('operations-ui.js',s);s=fs.readFileSync('index.html','utf8');const tag='<operations-panel v-if="page===\'inventory\'" ref="operations" class="inventory-loading-history"></operations-panel>';if(!s.includes(tag))throw Error('Missing history component');s=s.replace(tag,'');fs.writeFileSync('index.html',s);
