const fs=require('fs');let h=fs.readFileSync('index.html','utf8');
const start=h.indexOf('<div class="permission-grid">'),end=h.indexOf('<div v-if="!permissionGroups.length"',start);
if(start<0||end<0)throw Error('Permission template not found');
const template=`<div class="card permission-selection-bar"><label class="permission-select-all"><input type="checkbox" :checked="permissionSelection().all" :indeterminate.prop="permissionSelection().mixed" :disabled="!permissionEditable||!permissionSelection().total" @change="togglePermissionSelection(null,$event.target.checked)"><span><b>{{tr('تحديد كل الصلاحيات')}}</b><small>{{tr('يشمل جميع المجموعات حتى مع وجود بحث أو فلتر')}}</small></span></label><div class="actions"><span class="badge">{{permissionSelection().selected}} / {{permissionSelection().total}}</span><button class="btn small" :disabled="!permissionEditable" @click="togglePermissionSelection(null,false)">{{tr('إلغاء تحديد الكل')}}</button><button class="btn ghost small" :disabled="!permissionEditable" @click="resetPermissionSelection">{{tr('استعادة صلاحيات الدور')}}</button></div></div>
<p class="help permission-check-help">{{tr('ضع علامة ✓ للسماح، وأزلها للمنع. تحديد إجراء يفعّل عرض وحدته تلقائيًا؛ إلغاء عرض الوحدة يلغي إجراءاتها.')}}</p>
<div class="permission-grid permission-checkbox-grid"><section v-for="group in permissionGroups" :key="group.module" class="card permission-check-module" :data-permission-module="group.module"><div class="permission-check-header"><label class="permission-select-all"><input type="checkbox" :checked="permissionSelection(group.module).all" :indeterminate.prop="permissionSelection(group.module).mixed" :disabled="!permissionEditable||!permissionSelection(group.module).total" :aria-label="tr('تحديد كل صلاحيات')+' '+tr(group.title)" @change="togglePermissionSelection(group.module,$event.target.checked)"><span><b>{{tr(group.title)}}</b><small>{{tr('تحديد كل صلاحيات المجموعة')}}</small></span></label><span class="badge">{{permissionSelection(group.module).selected}} / {{permissionSelection(group.module).total}}</span></div><div class="permission-check-items"><label v-for="p in group.items" :key="p.key" class="permission-check-item" :class="{'is-selected':permissionEffective(p.key),'is-locked':permissionCheckDisabled(p)}"><input type="checkbox" :data-permission="p.key" :checked="permissionEffective(p.key)" :disabled="permissionCheckDisabled(p)" :aria-label="tr(group.title+' • '+p.label)" @change="togglePermissionCheck(p,$event.target.checked)"><span>{{tr(p.label)}}<small v-if="p.module==='backup'">{{tr('لمدير النظام فقط')}}</small></span><span v-if="p.sensitive" class="sensitive-dot" :title="tr('إجراء حساس')" aria-hidden="true">●</span></label></div></section></div>
`;
h=h.slice(0,start)+template+h.slice(end);fs.writeFileSync('index.html',h);
