const assert=require('node:assert/strict'),fs=require('fs');const {chromium}=require('C:/Users/PRO/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage(),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('file:///C:/Users/PRO/Desktop/'+encodeURIComponent('ماسال')+'/masal.html');for(const name of ['demo.main','demo.branch','demo.subbranch','demo.pos1','demo.pos2']){await p.locator('#login-name').fill(name);await p.locator('#login-password').fill('Demo123456789');await p.locator('.login-submit').click();await p.waitForFunction(()=>!app.loginScreen);assert.equal(await p.evaluate(()=>app.actor.username),name);await p.evaluate(()=>app.logoutToLogin());}const result=await p.evaluate(()=>{const s=app.s,e=new Masal.Engine(s,s.users[0].id);const accounts=['DEMO-MAIN','DEMO-BRANCH','DEMO-SUBBRANCH','DEMO-POS1','DEMO-POS2'];const before=accounts.map(a=>e.serviceBalance(a,'voucher'));const sales=[3,4].map((i)=>new Masal.Engine(s,'DEMO-U'+i).sell('DEMO-POS'+(i-2),'DEMO-PRODUCT',1,'DEMO-TEST-'+i));app.persist();return {before,after:accounts.map(a=>e.serviceBalance(a,'voucher')),sales:sales.map(t=>({qty:t.cards.length,agent:t.agent,pos:t.pos})),available:s.cards.filter(c=>c.status==='Available').length}});assert.deepEqual(result.before,[3360000,480000,240000,480000,240000]);assert.equal(result.available,998);assert.equal(result.after[3],475200);assert.equal(result.after[4],235200);await p.reload();assert.equal(await p.evaluate(()=>app.s.cards.filter(c=>c.status==='Available').length),998);assert.equal(await p.evaluate(()=>app.s.users.length),6);assert.deepEqual(errors,[]);console.log('PASS demo logins, funding conservation, both sale paths, persisted sales and no reseed',result);fs.writeFileSync('output/handoff/demo-verification.json',JSON.stringify({passed:true,result},null,2));}finally{await b.close()}})().catch(e=>{console.error(e);process.exitCode=1});
