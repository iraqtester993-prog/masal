const fs=require('fs');let t=fs.readFileSync('test-handoff.cjs','utf8').replace("app.s.users.length),1)","app.s.users.length),6)").replace("owner-only initial state","persistent demo initial state").replace("app.engine.toggleGovernorate('بغداد');","if(!app.s.governorates.find(g=>g.name==='بغداد')?.active)app.engine.toggleGovernorate('بغداد');").replace(".count(),5)",".count(),10)").replace("app.s.users.length),5)","app.s.users.length),10)");fs.writeFileSync('test-handoff.cjs',t);
