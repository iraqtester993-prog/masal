const {chromium}=require('C:/Users/PRO/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage(),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('file:///'+require('path').resolve('masal.html').replaceAll('\\','/'));await p.waitForSelector('.pagehead');await p.evaluate(()=>app.inspect({id:'LOG-TEST',time:'2026-09-20T11:43:48.781Z',user:'U1',name:'مدير النظام',action:'تصدير تقرير تفصيلي',entity:'reports',before:null,after:{sections:['sales'],rows:3,pin:'SECRET'},source:'جلسة محلية'}));const txt=await p.locator('.audit-detail-view').innerText();for(const label of ['المستخدم','الإجراء','قبل الإجراء','بعد الإجراء','المبيعات','عدد الصفوف','التقارير','بيانات محمية'])assert.ok(txt.includes(label),label);assert.ok(!txt.includes('SECRET'));assert.equal(await p.locator('.modal pre').count(),0);await p.setViewportSize({width:390,height:900});assert.equal(await p.evaluate(()=>document.documentElement.scrollWidth>innerWidth+2),false);assert.deepEqual(errors,[]);console.log('PASS Arabic audit details, nested report data, secret redaction, responsive layout');}finally{await b.close()}})().catch(e=>{console.error(e);process.exit(1)});
