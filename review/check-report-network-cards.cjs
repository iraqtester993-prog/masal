const {chromium}=require('C:/Users/PRO/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict'),{pathToFileURL}=require('node:url'),path=require('node:path');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage(),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto(pathToFileURL(path.resolve('masal.html')).href);await p.evaluate(()=>{app.enterLoginDemo();app.go('reports')});const r=await p.evaluate(async()=>{const all=app.reportKpis;app.reportCity='بغداد';const filtered=app.reportKpis;app.engine.toggleGovernorate('بغداد');const disabled=app.reportKpis;app.reportCity='';app.currentUser='U4';app.switchUser();app.go('reports');return {all,filtered,disabled,other:app.reportKpis}});assert.equal(r.all.length,10);assert.equal(r.filtered.at(-2).value,1);assert.equal(r.disabled.at(-2).value,0);assert(!r.other.some(x=>x.label==='المحافظات النشطة'));assert.deepEqual(errors,[]);console.log(JSON.stringify(r.all));console.log('PASS: existing six cards retained, four added; province filter and disabled state reflected; owner-only.')}finally{await b.close()}})().catch(e=>{console.error(e);process.exit(1)});
