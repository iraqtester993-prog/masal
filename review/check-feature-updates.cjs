const {chromium}=require('C:/Users/PRO/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage({viewport:{width:1440,height:1000}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto(require('node:url').pathToFileURL(path.resolve('masal.html')).href);await p.evaluate(()=>app.enterLoginDemo());
const result=await p.evaluate(async()=>{
 const result={},tick=()=>Vue.nextTick(),blocked=fn=>{try{fn();return false}catch{return true}};
 const rows=[{version:'1',agent:'A1',product:'C1',name:'اختبار',price:5000}];const blob=MasalExcel.workbook(rows),file=new File([blob],'prices.xlsx');const parsed=await MasalImportReader.read(file);result.excel=parsed[0].rows[1][0]==='1'&&parsed[0].rows[1][4]==='5000';
 app.go('prices');app.priceAgent='A1';await tick();const old=app.priceFor('A1','C1');await app.importPrices({target:{files:[file],value:'x'}});result.pricePreview=app.priceDraft.C1===5000&&app.priceFor('A1','C1')===old;
 await app.importPrices({target:{files:[new File([MasalExcel.workbook([...rows,...rows])],'duplicates.xlsx')],value:'x'}});result.duplicate=app.toast.text.includes('مكررة')&&app.priceDraft.C1===5000;
 await app.importPrices({target:{files:[new File([MasalExcel.workbook([{...rows[0],price:0}])],'invalid.xlsx')],value:'x'}});result.badPrice=app.toast.text.includes('غير صالح')&&app.priceDraft.C1===5000;
 await app.importPrices({target:{files:[new File([MasalExcel.workbook([{...rows[0],agent:'A2'}])],'other.xlsx')],value:'x'}});result.wrongAgent=app.toast.text.includes('غير صالح');
 const products=app.s.products,headers=['pin','serial','expiry'];const unknown={name:'unknown.csv',rows:[headers,['001','002','2028-01-01']]};result.unknown=!MasalFeatureUpdates.detect(unknown,products,[]).product;
 const rule={name:'vendor',headers,filenameMatch:'vendor-five',product:'C1'};result.rule=MasalFeatureUpdates.detect({...unknown,name:'vendor-five.csv'},products,[rule]).product==='C1';result.conflict=!MasalFeatureUpdates.detect({...unknown,name:'vendor-five.csv'},products,[rule,{...rule,product:'C2'}]).product;
 result.content=MasalFeatureUpdates.detect({name:'a.csv',rows:[['product','pin'],['C1','0001']]},products,[]).product==='C1';result.mixed=!MasalFeatureUpdates.detect({name:'a.csv',rows:[['product','pin'],['C1','0001'],['C2','0002']]},products,[]).product;
 app.go('import');await tick();const ops=app.$refs.operations;await ops.readFiles({target:{files:[new File(['pin,serial,expiry\n001,002,2028-01-01'],'unknown.csv')],value:'x'}});result.noDefault=ops.files[0].product==='';ops.previewFiles();result.noPreview=ops.preview.length===0;
 app.go('products');await tick();const before=app.filteredRows.map(p=>p.id);app.dragProduct=before[0];app.dropProduct(before[1]);await tick();result.order=app.filteredRows[1].id===before[0]&&app.s.products.find(x=>x.id===before[0]).order===2;
 const canvas=document.createElement('canvas');canvas.width=4;canvas.height=4;const image=canvas.toDataURL('image/png');app.go('support');await tick();app.openTicket();app.ticketForm.title='image support';app.ticketForm.description='description';app.ticketForm.image=image;app.saveTicket();await tick();const ticket=app.s.tickets.find(x=>x.title==='image support');result.support=!!ticket&&ticket.image===image&&!app.ticketForm.image;
 app.go('notifications');await tick();app.notificationForm={title:'image notice',body:'body',image};app.noticeMode='custom';app.noticeSelected=['U5'];app.sendNotification();await tick();result.notice=app.s.notifications.some(n=>n.title==='image notice'&&n.image===image&&n.recipientUsers.length===1)&&!app.notificationForm.image;
 result.badImage=blocked(()=>app.engine.sendNotice({title:'bad',body:'bad',image:'data:text/html;base64,YQ=='},'all',[]));
 app.currentUser='U5';app.switchUser();await tick();result.scopedNotice=app.visibleNotifications.some(n=>n.title==='image notice');result.denied=blocked(()=>app.engine.requirePermission('agents.export'));app.currentUser='U1';app.switchUser();await tick();app.go('agents');app.s.agents[0].phone='07701234567';app.s.agents[0].name='=SUM(1,2)';app.s.users[0].password='SECRET_CANARY';await tick();return result;
});for(const [k,v] of Object.entries(result))assert.equal(v,true,k);console.log(result);
const download=await Promise.all([p.waitForEvent('download'),p.evaluate(()=>app.exportCurrent())]);await download[0].saveAs('review/accounts-export-test.xlsx');
await p.evaluate(()=>app.go('notifications'));await p.locator('.attachment-field input[type=file]').setInputFiles({name:'pixel.png',mimeType:'image/png',buffer:Buffer.from('iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mP8/x8AAwMCAO+jU1sAAAAASUVORK5CYII=','base64')});await p.waitForFunction(()=>!app.attachmentBusy);assert.ok(await p.evaluate(()=>app.notificationForm.image));await p.screenshot({path:'review/notification-image-preview.png'});
await p.evaluate(()=>app.go('products'));await p.screenshot({path:'review/products-sort.png'});await p.setViewportSize({width:390,height:900});await p.screenshot({path:'review/products-sort-mobile.png'});assert.deepEqual(errors,[]);console.log('PASS feature updates, actual image upload and downloads');
}finally{await b.close()}})().catch(e=>{console.error(e);process.exit(1)});
