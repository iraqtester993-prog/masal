const {chromium}=require('C:/Users/PRO/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage({viewport:{width:1366,height:900}});const errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto(require('node:url').pathToFileURL(require('node:path').resolve('masal.html')).href);await p.evaluate(()=>{app.enterLoginDemo();app.go('inventory')});const agents=await p.evaluate(()=>app.inventoryAgents.map(a=>a.id));for(const id of agents){await p.getByLabel('مخزن الوكيل الرئيسي',{exact:true}).selectOption(id);const r=await p.evaluate(id=>{const cards=app.s.cards.filter(c=>c.agent===id&&c.status==='Available'&&c.expiry>Masal.day());return {expected:cards.length,actual:app.inventoryAvailableCards.length,cost:cards.reduce((s,c)=>s+c.cost,0),actualCost:app.inventoryAvailableCards.reduce((s,c)=>s+c.cost,0),rows:app.batchRows.every(b=>b.agent===id),invoices:app.$refs.operations.invoices.every(i=>i.agent===id),globalUntouched:app.availableCards.length>=cards.length}},id);assert.equal(r.actual,r.expected);assert.equal(r.cost,r.actualCost);assert.ok(r.rows&&r.invoices&&r.globalUntouched);assert.ok((await p.locator('.metrics').innerText()).includes(String(r.expected)))}await p.evaluate(()=>app.search='NO-MATCH');assert.equal(await p.evaluate(()=>app.inventoryCards.length),0);assert.equal(await p.getByText('لا توجد دفعات مطابقة',{exact:true}).count(),1);await p.evaluate(()=>{app.search='';app.inventoryAgent='';app.statusFilter='Quarantined'});assert.ok(await p.evaluate(()=>app.batchRows.every(b=>b.status==='Quarantined')&&app.inventoryAvailableCards.length===0));await p.evaluate(()=>{app.statusFilter='';app.inventoryAgent=app.inventoryAgents[0].id});await p.screenshot({path:'review/inventory-agent-filter.png'});await p.evaluate(()=>{app.currentUser=app.s.users.find(u=>u.role==='main').id;app.switchUser();app.go('inventory')});assert.equal(await p.evaluate(()=>app.inventoryAgent),'');assert.equal(await p.evaluate(()=>app.inventoryAgents.length),1);assert.ok(await p.evaluate(()=>app.batchRows.every(b=>app.engine.allowed(b.agent))));await p.setViewportSize({width:390,height:844});assert.ok(await p.evaluate(()=>document.documentElement.scrollWidth<=innerWidth+1));assert.deepEqual(errors,[]);console.log('PASS: per-agent stock/count/cost/invoices, combined search/status, empty result, account scope/reset, mobile bounds.');await b.close()})().catch(e=>{console.error(e);process.exit(1)});
