const {chromium}=require('C:/Users/PRO/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage();const errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto(require('node:url').pathToFileURL(require('node:path').resolve('masal.html')).href);await p.evaluate(()=>{app.enterLoginDemo();app.go('providers')});await p.waitForTimeout(400);assert.equal(await p.getByText('صورة التطبيق',{exact:true}).count(),0);assert.equal(await p.getByText('الجهة المنظمة',{exact:true}).count(),0);await p.evaluate(()=>app.openEdit());await p.locator('.overlay input').first().fill('شركة فحص');await p.evaluate(()=>{app.editForm.supplier='مجهز فحص';app.saveEntity()});assert.equal(await p.evaluate(()=>app.s.providers.some(x=>x.name==='شركة فحص')),true);await p.evaluate(()=>app.previewProvider(app.s.providers.find(x=>x.name==='شركة فحص')));await p.locator('.overlay').waitFor();assert.ok((await p.locator('.overlay').innerText()).includes('شركة فحص'));await p.evaluate(()=>{app.openEdit(app.s.providers.find(x=>x.name==='شركة فحص'));app.editForm.name='شركة معدلة';app.saveEntity()});assert.equal(await p.evaluate(()=>app.s.providers.some(x=>x.name==='شركة معدلة')),true);assert.deepEqual(errors,[]);console.log('PASS: companies render; removed fields absent; create without images, preview and edit work.');await b.close()})().catch(e=>{console.error(e);process.exit(1)});
