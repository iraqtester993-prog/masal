const {chromium}=require('C:/Users/PRO/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict'),path=require('node:path');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage({viewport:{width:1440,height:1000}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto(require('node:url').pathToFileURL(path.resolve('masal.html')).href);await p.evaluate(()=>{app.enterLoginDemo();app.go('permissions')});await p.getByRole('button',{name:'＋ إضافة صلاحية',exact:true}).click();await p.locator('.profile-name-card input:not([type=checkbox])').fill('موظف تقارير');await p.locator('.permission-modules button').filter({hasText:'مركز التقارير'}).click();await p.locator('[data-permission="reports.sales"]').check();await p.getByRole('button',{name:/حفظ نوع الصلاحية/}).click();await p.locator('.modal button').filter({hasText:'تأكيد حفظ نوع الصلاحية'}).click();const profile=await p.evaluate(()=>app.permissionUser);assert(profile);await p.evaluate(()=>app.go('users'));assert.deepEqual(await p.evaluate(()=>app.filteredRows.filter(u=>['main','sub','pos'].includes(u.role))),[]);assert.equal(await p.locator('.workspace-card table th').filter({hasText:'المعرف'}).count(),0);await p.getByRole('button',{name:'إضافة موظف',exact:true}).click();assert.equal(await p.locator('.staff-scope').count(),0);await p.locator('[name=employee-name]').fill('موظف جديد');await p.locator('[name=employee-email]').fill('newstaff@example.test');await p.locator('[name=employee-password]').fill('test-password-123');await p.locator('[name=employee-permission]').selectOption(profile);await p.getByRole('button',{name:/حفظ الموظف/}).click();await p.waitForFunction(()=>!app.modal);assert.equal(await p.evaluate(()=>app.s.users.find(u=>u.email==='newstaff@example.test').staffAccount),'@system');await p.reload();await p.waitForFunction(()=>typeof app!=='undefined');assert.equal(await p.evaluate(id=>app.s.permissionProfiles.find(p=>p.id===id).name,profile),'موظف تقارير');await p.evaluate(id=>{app.go('permissions');app.permissionUser=id;app.loadPermissions()},profile);await p.locator('.profile-name-card input[type=checkbox]').uncheck();await p.locator('.permission-save textarea').fill('إيقاف مؤقت');await p.getByRole('button',{name:/حفظ نوع الصلاحية/}).click();await p.locator('.modal button').filter({hasText:'تأكيد حفظ نوع الصلاحية'}).click();assert.equal(await p.evaluate(()=>MasalAccess.can(app.s.users.find(u=>u.email==='newstaff@example.test'),'reports.sales',app.s)),false);
for(const role of ['main','sub']){await p.evaluate(role=>{app.currentUser=app.s.users.find(u=>u.role===role).id;app.switchUser();app.go('permissions');app.newPermissionProfile()},role);await p.locator('.profile-name-card input:not([type=checkbox])').fill('صلاحية '+role);await p.locator('[data-permission="dashboard.view"]').check();await p.getByRole('button',{name:/حفظ نوع الصلاحية/}).click();await p.locator('.modal button').filter({hasText:'تأكيد حفظ نوع الصلاحية'}).click();assert.equal(await p.evaluate(()=>app.selectedPermissionProfile.ownerAccount===app.actor.agent),true);await p.evaluate(()=>{app.go('users');app.openEdit()});assert(await p.locator('[name=employee-permission] option').count()>1);await p.evaluate(()=>app.closeModal())}
await p.evaluate(()=>{app.currentUser='U1';app.switchUser();app.go('permissions');app.permissionModule='products'});for(const theme of ['light','dark'])for(const width of [1440,390]){await p.setViewportSize({width,height:1000});await p.evaluate(t=>{if(app.theme!==t)app.toggleTheme()},theme);assert.equal(await p.evaluate(()=>document.documentElement.scrollWidth>innerWidth+2),false);await p.screenshot({path:`review/staff-permissions-${theme}-${width}.png`})}assert.deepEqual(errors,[]);console.log('PASS staff-only list, no IDs, profile create/select/disable/persistence, main/sub profile ownership, light/dark desktop/mobile');}finally{await b.close()}})().catch(e=>{console.error(e);process.exit(1)});


